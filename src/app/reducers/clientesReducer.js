import {
    GET_CLIENTES,
    ADD_CLIENTE,
    UPDATE_CLIENTE, 
    REMOVE_CLIENTE
} from '../actions/types';

export default (state = {}, action) =>{
    switch(action.type){
        default:
            return state;
    }
}